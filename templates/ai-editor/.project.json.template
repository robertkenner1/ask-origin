{
  "name": "{{PROJECT_NAME}}",
  "description": "{{PROJECT_DESCRIPTION}}",
  "deployment": "vercel",
  "created": "{{CREATED_DATE}}",
  "repository": {
    "branch": "{{BRANCH_NAME}}"
  },
  "urls": {
    "local": "http://localhost:8181",
    "remote": "https://{{PROJECT_NAME}}-git-{{BRANCH_NAME}}-{{VERCEL_TEAM_SLUG}}.vercel.app"
  },
  "config": {
    "port": 8181,
    "vercelTeam": "{{VERCEL_TEAM_SLUG}}"
  },
  "template": {
    "type": "nextjs-app",
    "version": "1.0.0"
  },
  "environment": {
    "required": [
      {
        "key": "CLAUDE_API_KEY",
        "description": "Claude API key for AI features"
      }
    ]
  }
}
