{
  "name": "{{PROJECT_NAME}}",
  "version": "1.0.0",
  "description": "{{PROJECT_DESCRIPTION}}",
  "private": true,
  "scripts": {
    "dev": "http-server src -p 3000 -o -c-1",
    "build": "mkdir -p public && cp -r src/* public/",
    "start": "http-server public -p 3000",
    "clean": "rm -rf public"
  },
  "dependencies": {
    "@grammarly/design-system": "^7.0.1"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
