---
pagination_prev: null
pagination_next: null
---

import { ComponentStatusRow } from "../_components/component_status_row";
import Hero from "../_components/hero";
import useBaseUrl from "@docusaurus/useBaseUrl";
import PropsTable from "../_components/props_table";
import ComponentList from "../_components/component_list";
import ComponentPlayground from "../_components/comp_playground";
import { RadioGroup, RadioButton } from "@grammarly/design-system";
import RecommendationsTable from "../_components/recommendations_table";
import { ErrorRecs } from "../patterns/forms-pattern/forms.mdx";

# Radio Group

<span>A Radio Group allows a single selection from two or more options.</span>

<ComponentStatusRow name="Radio Group" />
<Hero
  path={useBaseUrl("/img/components/radio_group_hero.svg")}
  altText="A Radio Group allows a user to set the visibility of the style guide on different websites."
/>
<p class="caption">
  A Radio Group allows a user to set the visibility of the style guide on different websites.
</p>

## Usage

Use a Radio Group when a user can select a single option out of a set and it’s advantageous for them to see every option available.

**Do not use when:**

- It’s possible to select zero, one, or many options in a list. Instead, use [Checkboxes](/components/checkbox).
- There are more than six options. Instead, use [Select](/components/select).
- A single item can be turned on and off independently from other options and effects of the state change are immediate. Instead, use a [Switch](/components/switch).

## Anatomy

<div className="diagram p-8">
  <img
    src={useBaseUrl("/img/components/radio_group_anatomy.svg")}
    alt="Anatomy diagram of a Radio Group"
  />
</div>
<ol class="diagram-legend small">
  <li>Legend (required)</li>
  <li>Optional or Required indicator </li>
  <li>Helper text</li>
  <li>Radio Group (required)</li>
  <li>Error message</li>
</ol>

## Examples

### Column layout

The default `layout` for multiple Radio Buttons in a Radio Group is a two-column layout.

<ComponentPlayground
  code={`
    <RadioGroup legend="Visibility">
      <RadioButton value="all">All websites</RadioButton>
      <RadioButton value="select">Select websites only</RadioButton>
      <RadioButton value="popular">Popular websites only</RadioButton>
      <RadioButton value="editor">Editor websites only</RadioButton>
      <RadioButton value="blocked">Blocked websites only</RadioButton>
    </RadioGroup>
  `}
/>

### Row layout

A Radio Group can use a row layout by setting `layout="row"` when there are only two or three Radio Buttons or it’s preferable to save vertical space.

<ComponentPlayground
  code={`
    <RadioGroup legend="Visibility" layout="row">
      <RadioButton value="all">All websites</RadioButton>
      <RadioButton value="select">Select websites only</RadioButton>
      <RadioButton value="popular">Popular websites only</RadioButton>
    </RadioGroup>
  `}
/>

### With helper text

Use `helperMessage` to display additional details beyond the legend of the Radio Group. Each Radio Button within a Radio Group can also use `helperMessage` to provide more context.

<ComponentPlayground
  code={`
    <RadioGroup legend="Change your plan" layout="column" helperMessage="Select how you pay for Grammarly.">
      <RadioButton helperMessage="Billed once per year for $139.95" value="annual">Annually</RadioButton>
      <RadioButton helperMessage="Billed each quarter for $59.95" value="quarter">Quarterly</RadioButton>
      <RadioButton helperMessage="Billed each month for $29.98" value="monthly">Monthly</RadioButton>
    </RadioGroup>
  `}
/>

### With an error

Use `errorMessage` to display a red outline around the entire Radio Group and display an error message.

<ComponentPlayground
  code={`
    <RadioGroup legend="Visibility" errorMessage="Choose where this style guide is visible.">
      <RadioButton value="all">All websites</RadioButton>
      <RadioButton value="select">Select websites only</RadioButton>
      <RadioButton value="popular">Popular websites only</RadioButton>
      <RadioButton value="editor">Editor websites only</RadioButton>
      <RadioButton value="blocked">Blocked websites only</RadioButton>
    </RadioGroup>
  `}
/>

### Size

Use `size` to control the size of the Radio Group. The default size is `medium`.

<ComponentPlayground
  code={`
  <Flex gap={8} direction="column">
    <Flex gap={8}>
      <Text as="p">Medium</Text>
      <RadioGroup legend="Visibility" size="medium">
        <RadioButton value="all">All websites</RadioButton>
        <RadioButton value="select">Select websites only</RadioButton>
        <RadioButton value="popular">Popular websites only</RadioButton>
      </RadioGroup>
    </Flex>
    <Flex gap={8}>
      <Text as="p">Large</Text>
      <RadioGroup legend="Visibility" size="large">
        <RadioButton value="all">All websites</RadioButton>
        <RadioButton value="select">Select websites only</RadioButton>
        <RadioButton value="popular">Popular websites only</RadioButton>
      </RadioGroup>
    </Flex>
    <Flex gap={8}>
      <Text as="p">Xlarge</Text>
      <RadioGroup legend="Visibility" size="xlarge">
        <RadioButton value="all">All websites</RadioButton>
        <RadioButton value="select">Select websites only</RadioButton>
        <RadioButton value="popular">Popular websites only</RadioButton>
      </RadioGroup>
    </Flex>
  </Flex>
  `}
/>

### Required or optional indicator

Use `isRequired` or `isOptional` to add an indicator to the `legend`. Follow the Form pattern to decide when to note a field is required or optional. [Learn about required and optional indicators](/patterns/forms-pattern/forms#required-or-optional-indicator-1).

<ComponentPlayground
  code={`
    <Flex direction="column" gap={4}>
      <RadioGroup legend="Visibility" layout="row" isRequired>
        <RadioButton value="all">All websites</RadioButton>
        <RadioButton value="select">Select websites only</RadioButton>
        <RadioButton value="popular">Popular websites only</RadioButton>
      </RadioGroup>
      <RadioGroup legend="Visibility" layout="row" isOptional>
        <RadioButton value="all">All websites</RadioButton>
        <RadioButton value="select">Select websites only</RadioButton>
        <RadioButton value="popular">Popular websites only</RadioButton>
      </RadioGroup>
    </Flex>
  `}
/>

### Default selected item

Use `defaultValue` to set a Radio Button to be selected by default. This must match the `value` set on the Radio Button.

<ComponentPlayground
  code={`
    <RadioGroup legend="Visibility" defaultValue="select">
      <RadioButton value="all">All websites</RadioButton>
      <RadioButton value="select">Select websites only</RadioButton>
      <RadioButton value="popular">Popular websites only</RadioButton>
      <RadioButton value="editor">Editor websites only</RadioButton>
      <RadioButton value="blocked">Blocked websites only</RadioButton>
    </RadioGroup>
  `}
/>

### Disabled

Use `isDisabled` to indicate that a user cannot focus, change, or interact with a Radio Button or the entire Radio Group.

<ComponentPlayground
  code={`
    <Flex gap={6}>
      <RadioGroup legend="Visibility" isDisabled>
        <RadioButton value="all">All websites</RadioButton>
        <RadioButton value="select">Select websites only</RadioButton>
        <RadioButton value="popular">Popular websites only</RadioButton>
      </RadioGroup>
      <RadioGroup legend="Visibility" >
        <RadioButton value="all">All websites</RadioButton>
        <RadioButton value="select" isDisabled>Select websites only</RadioButton>
        <RadioButton value="popular">Popular websites only</RadioButton>
      </RadioGroup>
    </Flex>
  `}
/>

## Accessibility

### Hidden legends

A Radio Group’s legend can only be plain text. In some cases, you may require custom styling beyond what’s available as part of the component. For example, you might want the text to be a Heading. This requires visually hiding a Radio Group’s legend and adding a separate Heading component nearby.

A `legend` is still required, but use `legendDisplay="hidden"` to visually hide the legend while maintaining accessibility for people using screen readers. The hidden legend must include the visible text while providing any additional context if needed.

<ComponentPlayground
  code={`
    <div>
      <h3>Choose style guide visibility</h3>
      <RadioGroup legend="Visibility" legendDisplay="hidden">
        <RadioButton value="all">All websites</RadioButton>
        <RadioButton value="select">Select websites only</RadioButton>
        <RadioButton value="popular">Popular websites only</RadioButton>
        <RadioButton value="editor">Editor websites only</RadioButton>
        <RadioButton value="blocked">Blocked websites only</RadioButton>
      </RadioGroup>
    </div>
  `}
/>

### Disabling options

Use `isDisabled` on a Radio Group to indicate that all options within the group are unavailable. Use `isDisabled` on an individual Radio Button when only one or a few options are unavailable.

When a Radio Button or Group has the `isDisabled` attribute, it’s skipped in the keyboard navigation order. Users navigating this way will not know there is an unavailable item.

### Keyboard interaction

| Key                                             | Expected result                                                                                                                                                                                                                                            |
| ----------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <kbd>Tab</kbd> or <kbd>Shift+Tab</kbd>          | Moves focus into the Radio Group.<br></br><br></br>If a Radio Button is already selected, moves focus to that Radio Button. If a Radio Button is not selected, the focus lands on the first Radio Button (or last if moving backward through focus order). |
| <kbd>Down arrow</kbd> or <kbd>Right arrow</kbd> | Moves focus to the next Radio Button and selects the newly focused Radio Button.                                                                                                                                                                           |
| <kbd>Up arrow</kbd> or <kbd>Left arrow</kbd>    | Moves focus to the previous Radio Button and selects the newly focused Radio Button.                                                                                                                                                                       |
| <kbd>Space</kbd>                                | Selects or deselects the currently focused Radio Button.                                                                                                                                                                                                   |

## Writing

We aim to keep a consistent writing style across components within [Forms](/patterns/forms).

### Legend and labels

Include a legend that clearly describes the purpose of the entire Radio Group. The individual Radio Button labels are more specific and should clearly distinguish each choice from one another.

<RecommendationsTable
  caption="Writing best practices for Radio Group legends and Radio Button labels"
  recommendations={[
    {
      recommendationDo:
        "Keep the writing structure and verb tense consistent within a Radio Group or other Form components.",
      exampleDo: (
        <RadioGroup legend="Visibility">
          <RadioButton value="all">All websites</RadioButton>
          <RadioButton value="select">Select websites only</RadioButton>
          <RadioButton value="popular">Popular websites only</RadioButton>
        </RadioGroup>
      ),
      recommendationDont:
        "Do not mix writing structures or verb tenses within a Radio Group or other Form components.",
      exampleDont: (
        <RadioGroup legend="Visibility">
          <RadioButton value="all">Showing for all websites</RadioButton>
          <RadioButton value="select">Selected websites only</RadioButton>
          <RadioButton value="popular">Only allow on popular websites</RadioButton>
        </RadioGroup>
      ),
    },
    {
      recommendationDo:
        "Use language that adds specificity and builds on information given in other elements.",
      exampleDo: (
        <RadioGroup legend="Sharing">
          <RadioButton value="all">Only me</RadioButton>
          <RadioButton value="select">Specific groups</RadioButton>
          <RadioButton value="popular">Entire organization</RadioButton>
        </RadioGroup>
      ),
      recommendationDont: "Do not unnecessarily repeat information given in nearby elements.",
      exampleDont: (
        <RadioGroup legend="Sharing">
          <RadioButton value="all">Share with only me</RadioButton>
          <RadioButton value="select">Share with specific groups</RadioButton>
          <RadioButton value="popular">Share with entire organization</RadioButton>
        </RadioGroup>
      ),
    },
    {
      recommendationDo: "Use sentence case.",
      exampleDo: (
        <RadioGroup legend="Contact frequency">
          <RadioButton value="all">Daily</RadioButton>
          <RadioButton value="select">Weekly</RadioButton>
          <RadioButton value="popular">Monthly</RadioButton>
        </RadioGroup>
      ),
      recommendationDont: "Do not use Title Case or ALL CAPS.",
      exampleDont: (
        <RadioGroup legend="CONTACT FREQUENCY">
          <RadioButton value="all">DAILY</RadioButton>
          <RadioButton value="select">WEEKLY</RadioButton>
          <RadioButton value="popular">MONTHLY</RadioButton>
        </RadioGroup>
      ),
    },
    {
      recommendationDo: "Use internal punctuation when necessary.",
      exampleDo: (
        <RadioGroup legend="Search in which time period?">
          <RadioButton value="all">7-day history</RadioButton>
          <RadioButton value="select">14-day history</RadioButton>
          <RadioButton value="popular">30-day history</RadioButton>
        </RadioGroup>
      ),
      recommendationDont: "Do not use end punctuation, except for question marks in a legend.",
      exampleDont: (
        <RadioGroup legend="Select a date range!">
          <RadioButton value="all">7-day history.</RadioButton>
          <RadioButton value="select">14-day history.</RadioButton>
          <RadioButton value="popular">30-day history.</RadioButton>
        </RadioGroup>
      ),
    },
    {
      recommendationDo:
        "Use a single line for labels whenever possible. Text can wrap, but a shorter rewrite or helper text is preferable.",
      exampleDo: (
        <RadioGroup legend="Meeting preference">
          <RadioButton value="all">Morning</RadioButton>
          <RadioButton value="select">Lunchtime</RadioButton>
          <RadioButton value="popular">Afternoon</RadioButton>
        </RadioGroup>
      ),
      recommendationDont: "Do not truncate text.",
      exampleDont: (
        <RadioGroup legend="Meeting preference">
          <RadioButton value="all">Grouped in the morning</RadioButton>
          <RadioButton value="select">Consolidated in the mi …</RadioButton>
          <RadioButton value="popular">Clustered in the afte …</RadioButton>
        </RadioGroup>
      ),
    },
  ]}
/>

### Helper text

Helper text takes different forms to fit specific needs. Some tell a user what to do using action verbs, while others define terms and requirements with punchy fragments of information. [View examples of helper text](/patterns/forms-pattern/forms/#helper-text).

<RecommendationsTable
  caption="Writing best practices for helper text"
  textOnly={true}
  recommendations={[
    {
      recommendationDo:
        "In human terms, directly state how a user will benefit from giving their input or a reason we are requesting their input.",
      recommendationDont:
        "Do not use ambivalent or meandering language that softens feature benefits.",
    },
    {
      recommendationDo: "Define terms that may not be clear.",
      recommendationDont: "Do not use jargon or overly technical language that hinders clarity.",
    },
    {
      recommendationDo: "Make requirements clear to help prevent errors before they happen.",
      recommendationDont:
        "Do not assume that a user will understand how to complete an input without the addition of helper text. Inputs often have nuances that may confuse a user.",
    },
    {
      recommendationDo: `Use “Example:” to offer a universal and inclusive example of an expected input. It’s OK to provide both instructions and an example if space allows.`,
      recommendationDont: `Do not use “E.g.” or “I.e.” to indicate an example.`,
    },
    {
      recommendationDo: "Be brief. Use 140 characters or fewer unless unavoidable.",
      recommendationDont:
        "Do not overexplain, which can slow down a user with surplus information.",
    },
    {
      recommendationDo: "Use sentence case.",
      recommendationDont: "Do not use Title Case or ALL CAPS.",
    },
    {
      recommendationDo:
        "Include punctuation if it’s a complete sentence or to distinguish two fragments.",
      recommendationDont: "Do not use exclamation points.",
    },
  ]}
/>

### Error messages

<ErrorRecs />

## Installation

1. Import the CSS (if not done already).

```css
@import "@grammarly/design-system";
```

2. Import the Radio Group and Radio Button components in JS.

```tsx
import { RadioGroup, RadioButton } from "@grammarly/design-system";
```

## API

### RadioGroup props

<PropsTable
  properties={[
    {
      propertyName: "children",
      isRequired: true,
      typeDefinition: "React.ReactNode",
      description:
        "Most commonly will be a set of Radio Button components. Can be custom Radio Buttons only if absolutely necessary.",
    },
    {
      propertyName: "legend",
      isRequired: true,
      typeDefinition: "string",
      description:
        "Describes the purpose of the Radio Group. Can be visually hidden with `legendDisplay`.",
    },
    {
      propertyName: "defaultValue",
      typeDefinition: "string",
      description: "Defines if a Radio Button should be selected by default.",
    },
    {
      propertyName: "errorMessage",
      typeDefinition: "string",
      description:
        "Indicates the entire Radio Group is in an error state, adds error message below the input, and adds red border.",
    },
    {
      propertyName: "helperMessage",
      typeDefinition: "string",
      description: "Provides additional context outside of the legend of the Radio Group.",
    },
    {
      propertyName: "isDisabled",
      typeDefinition: "boolean",
      description:
        "When true, indicates that all Radio Buttons within this Radio Group cannot be focused, changed, or interacted with.",
    },
    {
      propertyName: "isOptional",
      typeDefinition: "boolean",
      description:
        "When true, adds “(Optional)” to the legend. Cannot be combined with `isRequired`.",
    },
    {
      propertyName: "isRequired",
      typeDefinition: "boolean",
      description:
        "When true, adds “(Required)” to the legend and marks input as required. Cannot be combined with `isOptional`.",
    },
    {
      propertyName: "layout",
      typeDefinition: "'column' | 'row'",
      defaultValue: "column",
      description:
        "Used to specify a column or row layout. In column format, will disperse Radio Buttons across two columns.",
    },
    {
      propertyName: "legendDisplay",
      typeDefinition: "'visible' | 'hidden'",
      defaultValue: "visible",
      description:
        "Can be used to visually hide the legend when another element on the page is acting as the visual label for the Radio Group.",
    },
    {
      propertyName: "size",
      typeDefinition: "'medium' | 'large' | 'xlarge'",
      defaultValue: "medium",
      description:
        "Controls the size of the Radio group and label.",
    },
    {
      propertyName: "onChange",
      typeDefinition: "(value: T) => void",
      description: "Event handler that is called when a Radio Button is selected.",
    },
  ]}
/>

### RadioButton props

<PropsTable
  properties={[
    {
      propertyName: "children",
      isRequired: true,
      typeDefinition: "string | React.ReactNode",
      description:
        "Used for the content of the Radio Button. When a string is passed, it will render using the GDS typography token `text-small`. When passing a Node, please match the style of any text by using `text-small` when applicable.",
    },
    {
      propertyName: "value",
      isRequired: true,
      typeDefinition: "string",
      description:
        "Used to identify this particular Radio Button. Must be unique within its sibling Radio Buttons.",
    },
    {
      propertyName: "helperMessage",
      typeDefinition: "string",
      description: "Provides additional context outside the label of an individual Radio Button.",
    },
    {
      propertyName: "isDisabled",
      typeDefinition: "boolean",
      description:
        "When true, indicates that an individual Radio Button cannot be focused, changed, or interacted with.",
    },
  ]}
/>

## Related components

export const FormComponents = [
  {
    href: "/components/checkbox",
    label: "Checkboxes",
  },
  {
    href: "/components/select",
    label: "Select",
  },
  {
    href: "/components/switch",
    label: "Switch",
  },
];

<ComponentList components={FormComponents} />
