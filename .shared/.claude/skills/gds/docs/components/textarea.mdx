---
pagination_prev: null
pagination_next: null
---

import { ComponentStatusRow } from "../_components/component_status_row";
import Recommendations from "../_components/recommendations";
import Hero from "../_components/hero";
import useBaseUrl from "@docusaurus/useBaseUrl";
import RecommendationsTable from "../_components/recommendations_table";
import ComponentList from "../_components/component_list";
import PropsTable from "../_components/props_table";
import { ErrorRecs } from "../patterns/forms-pattern/forms.mdx";
import ComponentPlayground from "../_components/comp_playground";
import { Button, Flex, Textarea, TextField, Text } from "@grammarly/design-system";

# Textarea

<span>A Textarea accepts multiple lines of text, numbers, or symbols.</span>

<ComponentStatusRow name="Textarea" />
<Hero
  path={useBaseUrl("/img/components/textarea_hero.svg")}
  altText="A Textarea captures the text used in a Snippet, which can range from short, direct messages to long-form emails depending on a user’s need."
/>
<p class="caption">
  A Textarea captures the text used in a Snippet, which can range from short, direct messages to
  long-form emails depending on a user’s need.
</p>

## Usage

Use when the expected text input is long-form content wrapped into multiple lines.

**Do not use when** a short, typed response is expected, such as an email address or password. Instead, use a [Text Field](/components/text-field).

## Anatomy

<div className="diagram p-8">
  <img
    src={useBaseUrl("/img/components/textarea_anatomy.svg")}
    alt="Anatomy diagram of a Textarea"
  />
</div>
<ol className="diagram-legend small">
  <li>Label (required)</li>
  <li>Helper text</li>
  <li>Optional or required indicator</li>
  <li>Textarea input (required)</li>
  <li>Error message</li>
</ol>

## Examples

### Default

By default, a Textarea is resizable, displays space for three rows of text in the input field, and includes a `label`.

<ComponentPlayground
  code={`
    <Textarea label="Snippet content"/>
  `}
/>

### Resizing

Use `resizable="off"` to prevent resizing of the input when it’s helpful for a user’s experience. You can also allow resizing in a single direction by specifying either `resizable="vertical"` or `resizable="horizontal"`.

<ComponentPlayground
  code={`
    <Flex gap={3}>
      <Textarea label="Snippet content" helperMessage="Not resizable" resizable="off"/>
      <Textarea label="Snippet content" helperMessage="Vertically resizable" resizable="vertical"/>
      <Textarea label="Snippet content" helperMessage="Horizontally resizable" resizable="horizontal"/>
    </Flex>
  `}
/>

### With helper text

Use `helperMessage` to display additional details beyond the label of a Textarea.

<ComponentPlayground
  code={`
    <Textarea label="Snippet content" helperMessage="Maximum: 5,000 characters"/>
  `}
/>

### With an error

Use `errorMessage` to change the color of the field’s outline to red and display an error message.

<ComponentPlayground
  code={`
    <Textarea label="Job description" isRequired errorMessage="Add a few examples of what you do."/>
  `}
/>

### Required or optional indicator

Use `isRequired` or `isOptional` to add an indicator to the `label`. Follow the Form pattern to decide when to note a field is required or optional. [Learn about required and optional indicators](/patterns/forms#required-or-optional-indicator-1).

<ComponentPlayground
  code={`
    <Flex gap={8}>
      <Textarea label="Snippet content" isRequired/>
      <Textarea label="Snippet content" isOptional/>
    </Flex>
  `}
/>

### Disabled

Use `isDisabled` to indicate that a user cannot focus, change, or interact with a Textarea.

<ComponentPlayground
  code={`
    <Textarea label="Job description" isDisabled/>
  `}
/>

### Default value

You can specify the `defaultValue` for an input when it’s useful.

<ComponentPlayground
  code={`
    <Textarea label="Snippet content" defaultValue="All the best, GDS"/>
  `}
/>

### Controlled value

The `value` of a Textarea can also be maintained through state.

<ComponentPlayground
  code={`
    <Textarea label="Improve it" value="The quick brown fox jumps over the lazy dog."/>
  `}
/>

### Events

The `onChange` event can be used to track changes to the input’s value. The `onFocus` event is called whenever a Textarea receives focus.

<ComponentPlayground
  code={`
    <Textarea 
      label="Tell us more"
      helperMessage="Add details about your issue." 
      onChange={( value ) => {
        alert("on change: " + value);
      }}
    />
  `}
/>

### With a ref

A `ref` can be set on a Textarea, which can help with focus management.

<ComponentPlayground
  code={`
  function Example() {
    const inputRef = React.useRef(null);
    return (
      <Flex gap={3} direction="column">
        <Textarea 
          ref={inputRef}
          label="About you"
        />
        <Button variant="secondary" text="Focus input" onClick={() => inputRef.current.focus()}/>
      </Flex>
    )
  }
  `}
/>

### In a form

A Textarea includes properties like `name` that can be used to track data in a form.

<ComponentPlayground
  code={`
    <Textarea name="improve-content" label="Improve it" value="The quick brown fox jumps over the lazy dog."/>
  `}
/>

## Behavior

### Size

The container of a Textarea determines its width.

By default, a Textarea displays space for three rows of text. Use the `rows` prop to set the initial height based on how much text a user is expected to write.

A Textarea does not automatically resize to display a user’s entire text as they type; however, a user can manually change the width and height of a Textarea by default. You can set `resizable="off"` to prevent resizing or alternatively allow resizing in a single direction by specifying either `resizable="vertical"` or `resizable="horizontal"`.

<ComponentPlayground
  code={`
    <Textarea rows={6} label="Improve it" resizable="horizontal"/>
  `}
/>

## Accessibility

### Hidden labels

A Textarea's label can only be plain text placed above the input. In some cases, you may require custom styling beyond what’s available as part of the component. For example, you might want to place the label to the side of the input instead of on top of it. This requires visually hiding a Textarea's label and adding a separate Text component nearby.

A `label` is still required, but `labelDisplay="hidden"` can be used to visually hide the label while maintaining accessibility for people using screen readers. Add an `onClick` event handler to the external, visible text that acts as the label. That `onClick` handler should call `.focus()` on the input.

<ComponentPlayground
  code={`
    <Flex gap={8} direction="column">
      <div style={{display: 'grid', gap: "36px", gridTemplateColumns: "1fr 200px"}}>
        <Text as="p" variant="text-small">Snippet name</Text>
        <TextField labelDisplay="hidden" label="Unique name for this snippet"/>
      </div>
      <div style={{display: 'grid', gap: "36px", gridTemplateColumns: "1fr 200px"}}>
        <Text as="p" variant="text-small">Snippet description</Text>
        <Textarea labelDisplay="hidden" label="Snippet description"/>
      </div>
    </Flex>
  `}
/>

### Keyboard interaction

| Key            | Expected result                    |
| -------------- | ---------------------------------- |
| <kbd>Tab</kbd> | Moves focus to the Textarea input. |

## Writing

We aim to keep a consistent writing style across components within [Forms](/patterns/forms).

### Labels

Labels help a user scan the various inputs in a Form. A label should describe the purpose of an individual input while clearly distinguishing each input from one another.

<RecommendationsTable
  caption="Writing best practices for Textarea labels"
  recommendations={[
    {
      recommendationDo:
        "Keep the writing structure and verb tense consistent within a set of Form components.",
      exampleDo: (
        <Flex gap={3} direction="column">
          <TextField label="Task" />
          <Textarea label="Description" />
        </Flex>
      ),
      recommendationDont:
        "Do not mix writing structures or verb tenses within a set of Form components.",
      exampleDont: (
        <Flex gap={3} direction="column">
          <TextField label="Task" />
          <Textarea label="Add a description, too." />
        </Flex>
      ),
    },
    {
      recommendationDo:
        "Use language that adds specificity and builds on information given in other elements.",
      exampleDo: (
        <Flex gap={3} direction="column">
          <Text as="p" variant="text-small" weight="bold">Add term</Text>
          <TextField label="Term" />
          <Textarea label="Description" />
        </Flex>
      ),
      recommendationDont: "Do not unnecessarily repeat information given in nearby elements.",
      exampleDont: (
        <Flex gap={3} direction="column">
          <Text as="p" variant="text-small" weight="bold">Add term</Text>
          <TextField label="New term" />
          <Textarea label="Term description" />
        </Flex>
      ),
    },
    {
      recommendationDo: "Use sentence case.",
      exampleDo: <Textarea label="About you" />,
      recommendationDont: "Do not use Title Case or ALL CAPS.",
      exampleDont: <Textarea label="ABOUT YOU" />,
    },
    {
      recommendationDo: "Use internal punctuation when necessary.",
      exampleDo: <Textarea label="Favorite stay-at-home activities" />,
      recommendationDont: "Do not use end punctuation, except for question marks.",
      exampleDont: <Textarea label="Favorite stay-at-home activities!" />,
    },
    {
      recommendationDo:
        "Use a single line for labels whenever possible. Text can wrap, but a shorter rewrite or helper text is preferable.",
      exampleDo: (
        <Textarea
          label="Show entire labels, even long ones"
          helperMessage="A little helper text goes a long way."
        />
      ),
      recommendationDont: "Do not truncate a label.",
      exampleDont: <Textarea label="Show entire labels, even …" />,
    },
  ]}
/>

### Helper text

Helper text takes different forms to fit specific needs. Some tell a user what to do with action verbs, while others define terms and requirements with punchy fragments of information.

<RecommendationsTable
  caption="Writing best practices for helper text"
  recommendations={[
    {
      recommendationDo:
        "In human terms, directly state how a user will benefit from their giving their input or a reason we are requesting their input.",
      exampleDo: (
        <Textarea label="Original text" helperMessage="Highlight or enter text to translate it." />
      ),
      recommendationDont:
        "Do not use ambivalent or meandering language that softens feature benefits.",
      exampleDont: (
        <Textarea
          label="Original text"
          helperMessage="You can choose to highlight or enter text to translate it."
        />
      ),
    },
    {
      recommendationDo: "Define terms that may not be clear.",
      exampleDo: (
        <Textarea label="Description" helperMessage="What your team should know about the term" />
      ),
      recommendationDont: "Do not use jargon or overly technical language that hinders clarity.",
      exampleDont: (
        <Textarea
          label="Description"
          helperMessage="The technical definition of a term that is displayed to employees of your corporate enterprise while typing text input in digital interfaces."
        />
      ),
    },
    {
      recommendationDo: "Make requirements clear to help prevent errors before they happen.",
      exampleDo: (
        <Textarea
          label="About you"
          helperMessage="Tell the world what makes you unique. Maximum: 2,000 characters."
        />
      ),
      recommendationDont:
        "Do not assume that a user will understand how to complete an input without the addition of helper text. Inputs often have nuances that may confuse a user.",
      exampleDont: <Textarea label="About you" />,
    },
    {
      recommendationDo: `Use “Example:” to offer a universal and inclusive example of an expected input. It’s OK to provide both instructions and an example if space allows.`,
      exampleDo: (
        <Textarea
          label="Snippet content"
          helperMessage="Example: a brand tagline or template email"
        />
      ),
      recommendationDont: `Do not use “E.g.” or “I.e.” to indicate an example.`,
      exampleDont: (
        <Textarea label="Snippet content" helperMessage="E.g., a brand tagline or template email" />
      ),
    },
    {
      recommendationDo: "Be brief. Use 140 characters or fewer unless unavoidable.",
      exampleDo: (
        <Textarea
          label="Tell us more"
          helperMessage="Add details about your issue. Our support team will get back to you within 48 hours."
        />
      ),
      recommendationDont:
        "Do not overexplain, which can slow down a user with surplus information.",
      exampleDont: (
        <Textarea
          label="Tell us more"
          helperMessage="Our team has a staff of 100 located in international hubs in order to provide support. after reviewing the details that you provide, we will cross-reference our resources and reply in a timely manner."
        />
      ),
    },
    {
      recommendationDo: "Use sentence case.",
      exampleDo: (
        <Textarea
          label="What do you want to know?"
          helperMessage="Example: What time is it in Berlin?"
        />
      ),
      recommendationDont: "Do not use Title Case or ALL CAPS.",
      exampleDont: (
        <Textarea
          label="What do you want to know?"
          helperMessage="EXAMPLE: What Time Is It in Berlin?"
        />
      ),
    },
    {
      recommendationDo:
        "Include punctuation if it’s a complete sentence or to distinguish two fragments.",
      exampleDo: (
        <Textarea
          label="Give feedback"
          helperMessage="Your feedback is always anonymous. Maximum: 2,000 characters."
        />
      ),
      recommendationDont: "Do not use exclamation points.",
      exampleDont: (
        <Textarea label="Give feedback!" helperMessage="Your feedback is always anonymous!" />
      ),
    },
  ]}
/>

### Placeholder text

In general, do not use placeholder text. To convey information necessary for understanding how to fill in a Textarea, use helper text that is accessible to a screen reader. [Learn how to write helper text](#helper-text).

<Recommendations
  recommendations={[
    {
      image: useBaseUrl("/img/components/textarea_placeholder_do.svg"),
      caption: "Use placeholder text rarely and only for nonessential information.",
      type: "do",
    },
    {
      image: useBaseUrl("/img/components/textarea_placeholder_dont.svg"),
      caption:
        "Do not use placeholder text for information necessary for understanding a Textarea.",
      type: "dont",
    },
  ]}
/>

### Error messages

<ErrorRecs />

## Installation

1. Import the CSS (if not done already).

```css
@import "@grammarly/design-system";
```

2. Import the Textarea component in JS.

```tsx
import { Textarea } from "@grammarly/design-system";
```

## API

<PropsTable
  properties={[
    {
      propertyName: "label",
      isRequired: true,
      typeDefinition: "string",
      description: "Displays the text label for a Textarea.",
    },
    {
      propertyName: "defaultValue",
      typeDefinition: "string",
      description: "Used to add prefilled text in a Textarea.",
    },
    {
      propertyName: "errorMessage",
      typeDefinition: "string",
      description:
        "Indicates a Textarea is in an error state, adds error message below the Textarea, and adds red border. When an error message is present, it is read by a screen reader after the label.",
    },
    {
      propertyName: "helperMessage",
      typeDefinition: "string",
      description:
        "Additional context for this Textarea outside of the label. The recommended max length for helper text is 140 characters.<br></br>`Aria-describedby` points to helper text (handled by `react-aria`).",
    },
    {
      propertyName: "isDisabled",
      typeDefinition: "boolean",
      description:
        "When true, indicates that the Textarea cannot be focused, changed, or interacted with.",
    },
    {
      propertyName: "isOptional",
      typeDefinition: "boolean",
      description:
        "When true, adds “(Optional)” to the label. Cannot be combined with `isRequired`.",
    },
    {
      propertyName: "isRequired",
      typeDefinition: "boolean",
      description:
        "When true, adds “(Required)” indicator to the label and marks input as required. Cannot be combined with `isOptional`.",
    },
    {
      propertyName: "labelDisplay",
      typeDefinition: "'visible' | 'hidden'",
      defaultValue: "visible",
      description:
        "Can be used to visually hide the label when another element on the page is acting as the visual label for the Textarea.",
    },
    {
      propertyName: "labelIndicatorForOptional",
      typeDefinition: "string",
      description: "Used to override the content for the optional indicator.",
      defaultValue: "(Optional)",
    },
    {
      propertyName: "labelIndicatorForRequired",
      typeDefinition: "string",
      description: "Used to override the content for the required indicator.",
      defaultValue: "(Required)",
    },
    {
      propertyName: "name",
      typeDefinition: "string",
      description:
        "Indicates the unique name for an input. Used for form validation. [Learn more at MDN](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#htmlattrdefname).",
    },
    {
      propertyName: "placeholderMessage",
      typeDefinition: "string",
      description:
        "Used to display placeholder text in a Textarea. Not recommended because this text disappears when a user starts to type.",
    },
    {
      propertyName: "resizable",
      typeDefinition: "'on' | 'off' | 'vertical' | 'horizontal'",
      defaultValue: "on",
      description: "Controls whether the Textarea is resizable and in which directions.",
    },
    {
      propertyName: "rows",
      typeDefinition: "number",
      defaultValue: 3,
      description: "Sets the number of visible rows.",
    },
    {
      propertyName: "value",
      typeDefinition: "string",
      description:
        "Indicates the current value of the input (controlled). Used for form validation.<br></br><br></br>For a controlled component, use the `onChange` event handler to set this prop appropriately. Do not use this property for for uncontrolled components.",
    },
    {
      propertyName: "onChange",
      typeDefinition: "(value: T) => void",
      description: "Event handler that is called when the Textarea value changes.",
    },
    {
      propertyName: "onFocus",
      typeDefinition: "(e: FocusEvent<Target>) => void",
      description: "Event handler that is called when the Textarea receives focus.",
    },
  ]}
/>

## Related components

<ComponentList
  components={[
    {
      href: "/components/text-field",
      label: "Text Field",
    },
  ]}
/>
