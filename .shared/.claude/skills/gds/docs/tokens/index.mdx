---
title: Tokens
sidebar_position: 2
pagination_prev: null
pagination_next: null
---

import DocCardList from "@theme/DocCardList";
import { useCurrentSidebarCategory } from "@docusaurus/theme-common";
import Link from "@docusaurus/Link";
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Tokens from "../_components/tokens_table.js";
import TokensTab from "../_components/tokens_tabs.js";
import InstallationLink from "./_installation_link.mdx";


# Tokens

  Tokens are the name and value pairs that store the Foundation visual styles used to create layouts
  and components. When applied consistently to the product, we’re able to meaningfully apply design
  decisions everywhere that customers experience Grammarly.


## Token implementation
It’s essential to apply tokens correctly to prevent any adverse effects in the future if a token's value changes. This means you shouldn’t use a border token as a background simply because it has the value you need. We strongly recommend working with a product designer or a GDS team member if you have questions about token usage in a specific context.

The design tokens are available as individual values in the Web (Javascript) library. To get started, follow the [installation instructions](/getting-started/engineers). Guidelines for integrating tokens on native platforms will be available soon.  
<br />
## All tokens
<TokensTab variantOverrides={{ "All tokens": "primary" }} />
<Tokens.TokenPage platform="web" label="Web (Javascript)" toc={toc} Link={InstallationLink} />
<DocCardList items={useCurrentSidebarCategory().items} />