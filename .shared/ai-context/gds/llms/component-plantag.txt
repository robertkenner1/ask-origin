# PlanTag

**Full path**: GDS/PlanTag

**Import**:
```typescript
import { PlanTag } from '@grammarly/design-system';
```

## Variants

- `free`
- `pro`
- `enterprise`
- `edu`
- `business`
- `premium`

## Sizes

- `xsmall`
- `small`
- `medium`
- `large`

## Examples

### Default

### AllVariantsAndSizes

```tsx
(
    <Flex gap={2}>
      {variants.map(variant => (
        <Flex key={variant} gap={2} direction="column">
          {sizes.map(size => (
            <PlanTag key={size} variant={variant} size={size} />
          )
```

### WithACustomTitle

```tsx
(
    <Flex gap={2}>
      {variants.map(variant => (
        <Flex key={variant} gap={2} direction="column">
          <PlanTag variant={variant} title={`${variant} feature`} />
        </Flex>
      )
```

## All Available Stories

- Default
- All Variants And Sizes
- With A Custom Title
- Visual Test

## Documentation

See the live Storybook for detailed usage examples, props, and interactive demos:
https://uifoundation.gpages.io/grammarly-design-system/?path=/docs/gds-plantag

## Related Components

Explore the full component library at: https://uifoundation.gpages.io/grammarly-design-system
