# Tag

**Full path**: GDS/Tag

**Import**:
```typescript
import { Tag } from '@grammarly/design-system';
```

## Variants

- `neutral`
- `brand`
- `critical`
- `premium`
- `pro`
- `success`
- `tip`
- `warning`
- `inverse`
- `deletion`
- `// deprecated
  addition`
- `// deprecated
  business`
- `// deprecated`

## DeprecatedVariant

- `deletion`
- `addition`
- `business`

## Examples

### Default

### AllVariants

```tsx
(
    <Flex gap={2}>
      {variants
        .filter(variant => !(deprecatedVariant as readonly string[])
```

### AllVariantsWithIcon

```tsx
(
    <Flex gap={2}>
      {variants
        .filter(variant => !(deprecatedVariant as readonly string[])
```

### PremiumProAndTipWithoutIcon

```tsx
(
    <Flex gap={2}>
      <Tag label="Tip" variant="tip" showIcon={false} />
      <Tag label="Premium" variant="premium" showIcon={false} />
      <Tag label="Pro" variant="pro" showIcon={false} />
    </Flex>
  )
```

### WithAccessibilityLabel

**Args**: `{"label":"\"Dog\"","accessibilityLabel":"\"Category: Dog\""}`

*... and 3 more examples*

## All Available Stories

- Default
- All Variants
- All Variants With Icon
- Premium Pro And Tip Without Icon
- With Accessibility Label
- In Narrow Container
- In Heading
- Subdued Background
- Visual Test

## Documentation

See the live Storybook for detailed usage examples, props, and interactive demos:
https://uifoundation.gpages.io/grammarly-design-system/?path=/docs/gds-tag

## Related Components

Explore the full component library at: https://uifoundation.gpages.io/grammarly-design-system
