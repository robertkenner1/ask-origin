# VerificationCode

**Full path**: GDS/VerificationCode

**Import**:
```typescript
import { VerificationCode } from '@grammarly/design-system';
```

## Examples

### Default

### AllSizes

```tsx
(
    <Flex gap={4} direction="column">
      <Text as="p">Small</Text>
      <VerificationCode size="small" legend="Security code" name="auth" />
      <Text as="p">Medium</Text>
      <VerificationCode size="medium" legend="Security code" name="auth" />
      <Text as="p">Large (default)
```

### AllSizesError

```tsx
(
    <Flex gap={4} direction="column">
      <Text as="p">Small</Text>
      <VerificationCode
        size="small"
        legend="Security code"
        name="auth"
        errorMessage="Required to connect account"
      />
      <Text as="p">Medium</Text>
      <VerificationCode
        size="medium"
        legend="Security code"
        name="auth"
        errorMessage="Required to connect account"
      />
      <Text as="p">Large (default)
```

### WithAnErrorMessage

**Args**: `{"errorMessage":"\"Enter a valid code\""}`

### WithAValue

```tsx
{
    const [value, setValue] = React.useState<string>("987654");

    return (
      <Flex direction="column" gap={6}
```

*... and 5 more examples*

## All Available Stories

- Default
- All Sizes
- All Sizes Error
- With An Error Message
- With A Value
- With A Value Alphanumeric
- With An On Change
- Disabled
- Assert Calls On Change With Current Value Typing
- Assert Calls On Focus
- Visual Test

## Documentation

See the live Storybook for detailed usage examples, props, and interactive demos:
https://uifoundation.gpages.io/grammarly-design-system/?path=/docs/gds-verificationcode

## Related Components

Explore the full component library at: https://uifoundation.gpages.io/grammarly-design-system
