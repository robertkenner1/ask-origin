# Select

**Full path**: GDS/Select

**Import**:
```typescript
import { Select } from '@grammarly/design-system';
```

## Examples

### Default

### WithOnChange

```tsx
{
    const [selection, setSelection] = React.useState<string>("dog");

    return (
      <Flex gap={1}
```

### WithoutDefaultValue

```tsx
{
    const [selection, setSelection] = React.useState<string>("");

    return (
      <Flex gap={1}
```

### WithOptionGroups

```tsx
{
    // demonstrating optgroup, option.label, option.disabled
    return (
      <Select label="Favorite animal">
        <optgroup label="Wild">
          <Select.Option value="penguin" label="Penguin" />
          <Select.Option value="lion" label="Lion" disabled />
          <Select.Option value="tiger" label="Tiger" />
        </optgroup>
        <optgroup label="Domesticated">
          <Select.Option value="cat">Cat</Select.Option>
          <Select.Option value="dog">Dog</Select.Option>
...
```

### WithinShadowDOM

## All Available Stories

- Default
- With On Change
- Without Default Value
- With Option Groups
- All States
- Within Shadow DOM
- Visual Test

## Documentation

See the live Storybook for detailed usage examples, props, and interactive demos:
https://uifoundation.gpages.io/grammarly-design-system/?path=/docs/gds-select

## Related Components

Explore the full component library at: https://uifoundation.gpages.io/grammarly-design-system
